plugins {
    id 'java-library'
    id 'io.quarkus'
}

dependencies {
    implementation enforcedPlatform("${quarkusPlatformGroupId}:${quarkusPlatformArtifactId}:${quarkusPlatformVersion}")
    api project(":shared:testing-utils")
    api 'io.quarkus:quarkus-junit5'
}
